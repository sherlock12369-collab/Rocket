# 🚀 Rocket Family Mall - 시스템 명세서

'로켓 패밀리몰'은 가족 구성원 전용 포인트 쇼핑몰로, 심우주(Deep Space) 테마의 현대적인 UI와 실시간 포인트 거래 로직을 갖춘 웹 애플리케이션입니다.

---

## 🛠️ 기술 스택 (Tech Stack)

- **프레임워크**: Vue 3 (Composition API)
- **빌드 도구**: Vite
- **백엔드**: Node.js (Express)
- **데이터베이스**: MongoDB Atlas (Cloud)
- **인증**: JWT (JSON Web Token) 및 Bcrypt.js
- **스타일링**: Tailwind CSS 4 (Samsung Premium Modern Style)
- **언어**: TypeScript

---

## 🛡️ 인증 및 권한 (Auth & Roles)

### 1. 역할분담
- **Commander (Admin)**: 상품 등록/수정/삭제, 모든 주문 내역 관리 권한.
- **Explorer (User)**: 상품 조회, 장바구니 담기, 주문(포인트 결제), 본인 프로필 관리.

### 2. 계정 정보 (초기 세팅)
- **관리자**: `sherlock` / `rocket1234`
- **시드 데이터**: `/api/seed` 경로 접속 시 초기 관리자 계정이 생성됩니다.

---

## 📦 상품 관리 (Product Management)

### 1. 고급 이미지 업로드
- **URL 방식**: 기존 웹 이미지를 주소로 등록.
- **파일 업로드**: 로컬 이미지를 직접 선택하여 등록 (Base64 인코딩 후 DB 저장).
- **자동 대체**: 이미지를 등록하지 않거나 로드 실패 시 '로켓 보일러플레이트' 이미지 자동 표시.

### 2. 유연한 필드 구성
- **설명(Description)**: 선택 사항으로 변경하여 빠른 상품 등록 가능.
- **유형(Type)**: '구매(Buy)'와 '대여(Rent)' 구분 지원.
- **재고(Stock)**: 실시간 재고 기반 품절 처리 로직 적용.

---

## 💰 경제 및 주문 로직 (Economy & Orders)

### 1. 포인트 거래 시스템
- 주문 시 사용자의 포인트(`pointBalance`)를 실시간으로 차감합니다.
- 상품의 재고가 부족하거나 포인트가 모자랄 경우 주문을 원천 차단하는 정합성 검증 포함.

### 2. 배송비 정책
- **기본 배송비**: 2 P
- **무료 배송**: 총 주문 금액 15 P 이상 시 자동 적용.

### 3. 주문 상태 (Status)
- **Pending(승인 대기)**: 주문 직후 상태.
- **Approved(배송 중)**: 관리자 승인 후.
- **Fulfilled(배송 완료)**: 상품 수령 완료.
- **Rejected(반려)**: 관리자에 의한 주문 취소 (포인트 환불 로직 연동 가능).

---

## 🎨 디자인 시스템 (Premium UI/UX)

### 1. 시각적 테마
- **컨셉**: Samsung Premium Modern Style
- **색상**: Stark White (#FFFFFF), Samsung Blue (#007AFF), Deep Black (#000000).
- **스타일**: 미니멀리즘, Pill-shape 버튼, 얇은 테두리(Hairline border), 넉넉한 여백(Negative Space).

### 2. 사용자 인터랙션
- **마이크로 애니메이션**: 부드러운 호버 효과 및 스케일 애니메이션.
- **반응형 디자인**: 모바일과 데스크톱 모두에서 고도로 최적화된 레이아웃.

---

## 🚀 향후 확장 계획 (Roadmap)
- [x] 부모 관리자 페이지 (주문 승인/반려 UI 고도화)
- [x] 포인트 충전 및 활동 미션 연동
- [x] 카테고리별 필터링 기능 추가
- [x] 실시간 알림 시스템 (Toast Notification)
